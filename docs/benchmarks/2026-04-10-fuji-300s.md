# Benchmark: avalanche-rs vs AvalancheGo v1.14.2

- Date (UTC): 2026-04-10T22:40:29Z
- Network: Fuji testnet
- Duration: 300s per implementation
- Hardware: Darwin arm64
- Bootstrap IP: 52.29.72.46:9651
- Bootstrap ID: NodeID-7Xhw2mDxuDS44j42TCB6U5579esbSt3Lg
- avalanche-rs revision: local M7 working tree based on 09b5bb8
- AvalancheGo version: v1.14.2

This was a real cold-start benchmark against the shared Fuji production surface. The harness was fixed before this run so each implementation is measured against the same 300-second wall-clock budget from process start.

## Cold Start Sync

| Metric | avalanche-rs | AvalancheGo v1.14.2 |
|--------|-------------|---------------------------|
| Binary size | 10272 KB | 88968 KB |
| First peer handshake | 330258 ms | NA ms |
| Final RSS | 12848 KB | 75696 KB |
| Peak RSS | 121936 KB | 91264 KB |
| P-Chain height | 273056 | null |
| C-Chain block number | 0x336e5ac | null |
| Peer count (`info.peers`) | 1 | 42 |
| P-Chain bootstrapped | true | false |
| Health endpoint healthy | false | false |

`avalanche-rs` now advances C-Chain canonical headers during cold start. It intentionally keeps health/readiness false because those C-Chain blocks are imported in header-only mode until full C-Chain state sync is implemented. AvalancheGo connected to substantially more peers, but did not expose usable P/C chain RPC results inside the same 300-second cold-start window.

## Shared RPC Latency Slice

Average of 3 requests after the 5-minute run.

| Endpoint | avalanche-rs | AvalancheGo v1.14.2 |
|----------|-------------|---------------------------|
| `eth_blockNumber` | 0.5 ms | 0.8 ms |
| `eth_chainId` | 0.4 ms | 0.5 ms |
| `eth_gasPrice` | 0.6 ms | 0.7 ms |
| `platform.getHeight` | 0.4 ms | 0.6 ms |
| `GET /ext/health` | 0.5 ms | 0.7 ms |
| `GET /metrics` | NA ms | 2.6 ms |

## Shared RPC Values At End Of Run

| Value | avalanche-rs | AvalancheGo v1.14.2 |
|-------|-------------|---------------------------|
| `eth_chainId` | 0xa86a | null |
| `eth_gasPrice` | 0x60db88400 | null |

## Raw Artifacts

- avalanche-rs log: `./docs/benchmarks/raw/20260410T222721Z-avalanche-rs.log`
- avalanche-rs RSS samples: `./docs/benchmarks/raw/20260410T222721Z-avalanche-rs-rss.csv`
- AvalancheGo log: `./docs/benchmarks/raw/20260410T222721Z-avalanchego.log`
- AvalancheGo RSS samples: `./docs/benchmarks/raw/20260410T222721Z-avalanchego-rss.csv`

## Scope Notes

- This benchmark is a real 5-minute cold-start comparison against the shared Fuji production surface.
- It measures sync progress, resource usage, health, and a shared RPC slice. It does not attempt to benchmark every non-overlapping feature family one-by-one inside the same 5-minute window.
- Both runs start from empty local state and are executed sequentially on the same host.
- `avalanche-rs` C-Chain stateful correctness is not complete in this run: current head tracking works, but account/storage/receipt correctness after cold start still requires full C-Chain state sync rather than header-only import.
